import { IsErrorDirective } from './is-error.directive';

describe('IsErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new IsErrorDirective();
    expect(directive).toBeTruthy();
  });
});
