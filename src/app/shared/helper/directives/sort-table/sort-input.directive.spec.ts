import { SortInputDirective } from './sort-input.directive';

describe('SortInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SortInputDirective();
    expect(directive).toBeTruthy();
  });
});
